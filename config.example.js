// Optional future production config file.
// This prototype uses public no-key APIs. Add your own keys/proxies here if needed.
window.DIGITAL_TWIN_CONFIG = {
  apiProxyBaseUrl: '',
  refreshMinutes: 10
};
